# Example environment file (DO NOT COMMIT REAL SECRETS)
## Copy to backend/.env for local dev or set these in Render's Environment panel.

# REQUIRED
SUPABASE_URL="https://YOUR_PROJECT.supabase.co"
SUPABASE_SERVICE_KEY="YOUR_SERVICE_ROLE_KEY"  # Prefer service key for writes
# If you truly cannot use service role, set SUPABASE_ANON_KEY as a fallback (writes may fail):
# SUPABASE_ANON_KEY="YOUR_ANON_KEY"

# YouTube Data API v3 — comma-separated; recommend multiple keys for rotation
YOUTUBE_API_KEYS="key1,key2,key3"

# OPTIONAL
LOG_LEVEL=info
NODE_ENV=production
PORT=8080
REGION_MATRIX_PATH=./regions.json
# Toggle richer column writes after DB migration is applied (see README)
# SUPABASE_RICH_SCHEMA=1
# Reset budget/quotas (debugging only)
# FORCE_BUDGET_RESET=false
# CACHE_TTL_DAYS=7
