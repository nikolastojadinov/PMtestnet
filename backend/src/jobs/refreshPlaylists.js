export async function runRefreshPlaylists({ reason = 'manual' } = {}) {
  console.log(`[refresh] start (${reason}) @ ${new Date().toISOString()}`);
  // real implementation added in Prompt 4
  console.log('[refresh] (stub) complete');
}
