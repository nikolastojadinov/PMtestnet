export async function runFetchPlaylists({ reason = 'manual' } = {}) {
  console.log(`[fetch] start (${reason}) @ ${new Date().toISOString()}`);
  // real implementation added in Prompt 3
  console.log('[fetch] (stub) complete');
}
